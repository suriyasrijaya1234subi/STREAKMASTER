var app = angular.module('streakMasterApp', []);

app.controller('MainController', ['$scope', function($scope) {
    // Additional logic for handling login/register, etc., can be added here.
}]);
